{% extends 'base.html.twig' %}

{% block title %}CityBreakDetails{% endblock %}

{% block body %}
    <h1>CityBreakDetails</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ city_break_detail.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('city_break_details_index') }}">back to list</a>

    <a href="{{ path('city_break_details_edit', {'id': city_break_detail.id}) }}">edit</a>

    {{ include('city_break_details/_delete_form.html.twig') }}
{% endblock %}
