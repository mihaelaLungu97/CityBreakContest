{% extends 'base.html.twig' %}

{% block title %}CityBreakDetails index{% endblock %}

{% block body %}
    <h1>CityBreakDetails index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for city_break_detail in city_break_details %}
            <tr>
                <td>{{ city_break_detail.id }}</td>
                <td>
                    <a href="{{ path('city_break_details_show', {'id': city_break_detail.id}) }}">show</a>
                    <a href="{{ path('city_break_details_edit', {'id': city_break_detail.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('city_break_details_new') }}">Create new</a>
{% endblock %}
